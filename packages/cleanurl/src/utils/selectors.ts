import { CLEAN_URL_ATTRIBUTE, generateSelectors } from '@finsweet/attributes-utils';

import { ELEMENTS, SETTINGS } from './constants';

export const {} = generateSelectors(CLEAN_URL_ATTRIBUTE, ELEMENTS, SETTINGS);
